<!-- Machine anchors: the four dashboard blocks are delimited by
     `<!-- block:NAME -->` … `<!-- /block:NAME -->`. Address them by
     MARKER, never by heading text — headings are translated into the
     vault language, markers never are. Refreshing a block means
     replacing only the lines BETWEEN its marker pair; markers,
     headings and everything below the `---` stay untouched. Lines
     carrying `<!-- keep:… -->` survive every refresh — update their
     content, never delete the line. Agents looking for paths rather
     than prose: read index.md instead. -->

This vault at a glance. You can't get lost: every path leads back here.
(Claude fills and updates this page. No heading here on purpose —
Obsidian already shows the filename as the title.)

## Right now
<!-- block:right-now -->
<!-- Claude: what is currently being changed in this vault — new or reworked SOPs, roles being filled, partners under review. One line each, wikilink + status. The "Roles:" line below is the entry into 60-roles/ — refresh its links, never drop the line. -->
- *(fills up as the vault is put to work)*
- <!-- keep:areas-line -->Roles: *(the roles described in `60-roles/` appear here)*
<!-- /block:right-now -->

## Next deadlines
<!-- block:next-deadlines -->
<!-- Claude: mirror the next 3 dates from Deadlines.md; overdue `review_due:` fields belong here too -->
- *(none yet — every date lives in [[Deadlines]])*
<!-- /block:next-deadlines -->

## Open questions
<!-- block:open-questions -->
<!-- Claude: contradictions, unowned notes, and suggestions waiting for release in 00-inbox/suggestions/ -->
- *(fills up as the vault learns what it doesn't know)*
<!-- /block:open-questions -->

## New this week
<!-- block:new-this-week -->
<!-- Claude: the 3-5 most recently released or reworked notes as wikilinks, refreshed at every review -->
- *(updated at every review)*
<!-- /block:new-this-week -->

---

## How to use this vault
- **Something is wrong or missing** → write it into `00-inbox/suggestions/`. It is a proposal until someone releases it — that is the whole point.
- **New here** → [[onboarding-path]] (first hour to day 90)
- **Ask it something** → *"what does this vault say about …?"*
- **Once a week, fixed day** → `brain review` (inbox and suggestions to zero)
- About: [[About this vault]] · Roles: `60-roles/` · Rules: [[CLAUDE|CLAUDE.md]]

*A note counts as company truth only with `status: stable` and a filled
`verified:`. Everything else is somebody's proposal.*
